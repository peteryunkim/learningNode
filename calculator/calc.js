let add = (num1, num2) => {
	console.log( num1 + num2 )
}

let subtract = (num1, num2) => {
	console.log( num1 - num2 )
}

let multiply = (num1, num2) => {
	console.log( num1 * num2 )
}

let divide = (num1, num2) => {
	console.log( num1 / num2 )
}

module.exports = {
	add: add,
	subtract: subtract,
	multiply: multiply,
	divide: divide
	}